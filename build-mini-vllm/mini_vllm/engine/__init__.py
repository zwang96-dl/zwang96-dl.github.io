"""engine/ —— 把各部件组装成完整推理引擎（轨道 B）。

计划在 Phase 3 落地（Lesson 17）：
    model_runner.py —— 组织一次 forward 的输入/输出
    engine.py       —— engine loop：调度 → 分配 → 运行 → 采样 → 回收
    llm.py / cli.py —— 面向用户的入口与命令行

Phase 1 暂为占位。
"""
