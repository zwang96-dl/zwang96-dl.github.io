"""multimodal/ —— 多模态输入处理（Phase 4~6）。

计划落地（Lesson 19~30）：
    inputs.py / messages.py / chat_template.py —— 结构化消息与模板
    placeholders.py                            —— placeholder range 与 expansion
    media.py / image_processor.py / video_sampler.py —— 媒体加载与预处理
    patch_embed.py / vision_encoder.py / projector.py —— 视觉编码与投影
    embedding_merge.py                         —— 文本/视觉 embedding 合并
    cache.py / budget.py / runner.py           —— 三层缓存、visual token 预算、runner

Phase 1 暂为占位。多模态建立在文本引擎（Phase 3）完成之后。
"""
